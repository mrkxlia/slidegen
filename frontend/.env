# 公開してよいフロント設定（コミットする）。秘密はここに置かない。
# API ベース: 空ならローカルは vite proxy(/api→:8787)、本番は同一オリジン(/api)を叩く。
VITE_API_BASE=
# Pyodide ランタイムの配信元（CDN）。自己ホストする場合は差し替える。
VITE_PYODIDE_URL=https://cdn.jsdelivr.net/pyodide/v0.28.3/full/
# VITE_WHEEL_URL は tools/build_wheel.sh が .env.local に自動生成する。
